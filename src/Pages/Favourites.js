import React from "react";
import Theme from "../Components/Theme";
import NavBottom from "../Components/NavBottom";

const Favourites = () => {
	return (
		<>
			<Theme>
				<div>This is Favourites page</div>
			</Theme>
			<NavBottom/>
		</>
	);
};

export default Favourites;
